#include <stdio.h>
#include <stdlib.h>
/**
 * main - a program that prints all single digit munbers of base 10
 * Return: 0
 */
int main(void)
{
	int n = 0;

	while (n < 10)
	{
	putchar(n);
	}
	putchar('\n');
	return (0);
}
