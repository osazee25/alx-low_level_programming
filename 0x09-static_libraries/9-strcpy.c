#include "main.h"
#include <stdio.h>
#include <stdlib.h>
#include <ctype.h>
#include <string.h>
/**
 * _strcpy - copies a string
 * @dest: dest
 * @src: src
 * Return: char
 */
char *_strcpy(char *dest, char *src)
{
	return (strcpy(dest, src));
}
